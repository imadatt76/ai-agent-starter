# AI Collaboration Rules

## 目的

このワークスペース内でのAIとの会話を、保存された情報に基づく形に固定する。
速度より文脈整合性と再現性を優先する。

---

## 最優先ルール

1. 回答前に、まずこのワークスペース内の既存情報を参照する。
   - `INDEX.md`
   - `OS.md`
   - `COMMAND.md`
   - `wiki/index.md`
   - 関連する `wiki/pages/`
2. 直前メッセージだけで断定しない。
3. 一般論を使う場合は、この環境の情報とどう接続するかを示す。
4. 重要な助言・判断提案では、根拠に使ったファイル名を明記する。

---

## 禁止

- ノリや雰囲気だけで断定すること
- ワークスペース情報を未確認のまま「あなた向け」と言い切ること
- 不安を煽ること
- 相手の判断権を奪うこと
- 短期売上だけを優先すること

---

## 応答プロトコル

1. 依頼内容を要約する。
2. 参照すべきローカル情報を確認する。
3. 根拠ベースで回答する。
4. 不確実性がある場合は、推測箇所を明示する。
5. 次の行動は3つ以内に絞る。

---

## ログ保存プロトコル

ユーザーが「ログ残して」「記録しといて」「このやり取り残して」と依頼した場合は、会話内容を `logs/casual/` に保存する。

保存形式はJSONを推奨し、最低限以下を含める。

- `id`
- `saved_at`
- `source_ai`
- `theme`
- `self_insights`
- `business_ideas`
- `content_ideas`
- `raw_log`

`raw_log` は全文の逐語記録ではなく、後で文脈復元できる粒度の要約にする。

---

## ファイル書き込みルール

- Markdown、JSON、CSVはUTF-8で保存する。
- JSON保存後は、読み込めることを確認する。
- 秘密情報、APIキー、認証情報、個人情報を公開リポジトリに保存しない。

---

## 実行環境メモ

- Pythonを使える環境では、集計・ログ検証・整形にPythonを使ってよい。
- `python` や `conda` がPATHで見つからない場合でも、すぐに「Pythonなし」と判断しない。
- Python実行が必要な場合は、conda環境、同梱Python、利用可能なランチャーを順に確認する。
